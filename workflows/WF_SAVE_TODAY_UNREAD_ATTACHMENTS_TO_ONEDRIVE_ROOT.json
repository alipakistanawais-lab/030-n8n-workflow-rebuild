{
  "name": "WF_SAVE_TODAY_UNREAD_ATTACHMENTS_TO_ONEDRIVE_ROOT",
  "nodes": [
    {
      "parameters": {
        "workflowInputs": {
          "values": [
            {
              "name": "telegramChatId"
            }
          ]
        }
      },
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        0
      ],
      "id": "e153d9d7-26b0-4a3c-a46e-268116b3c0b5",
      "name": "When Executed by Another Workflow"
    },
    {
      "parameters": {
        "jsCode": "\nfunction safeText(value) { return String(value === undefined || value === null ? '' : value).trim(); }\nfunction num(value, fallback) { const n = Number(value); return Number.isFinite(n) ? n : fallback; }\nfunction berlinNow() {\n  const f = new Intl.DateTimeFormat('en-CA', { timeZone: 'Europe/Berlin', year:'numeric', month:'2-digit', day:'2-digit', hour:'2-digit', minute:'2-digit', second:'2-digit', hour12:false });\n  const p = {}; for (const part of f.formatToParts(new Date())) { if (part.type !== 'literal') p[part.type] = part.value; }\n  return `${p.year}-${p.month}-${p.day}T${p.hour}:${p.minute}:${p.second}`;\n}\nfunction httpError(item) {\n  const j = item && item.json ? item.json : {};\n  if (j.error) {\n    const e = j.error.error || j.error;\n    return (e && (e.message || e.code)) ? (e.message || e.code) : 'Microsoft Graph request failed';\n  }\n  return '';\n}\n\nconst now = berlinNow();              // YYYY-MM-DDTHH:mm:ss in Berlin\nconst day = now.slice(0,10);\nconst start = day + 'T00:00:00';      // start of today (Berlin local, naive)\nreturn [{ json: { startNaive: start } }];\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        0
      ],
      "id": "349cdaf6-c583-4f23-b0bf-d407a6a10506",
      "name": "00 Prepare Today Range"
    },
    {
      "parameters": {
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "options": {},
        "url": "={{ 'https://graph.microsoft.com/v1.0/me/mailFolders/Inbox/messages?$top=25&$filter=isRead eq false and hasAttachments eq true and receivedDateTime ge ' + $json.startNaive + 'Z&$select=id,subject,receivedDateTime&$expand=attachments($select=id,name,contentType,size,isInline)' }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Accept",
              "value": "application/json"
            },
            {
              "name": "Prefer",
              "value": "outlook.timezone=\"Europe/Berlin\""
            }
          ]
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        440,
        0
      ],
      "id": "1dc78ed4-b26f-4f46-a488-744b4c9ea3e9",
      "name": "01 Get Today Unread With Attachments",
      "credentials": {
        "oAuth2Api": {
          "id": "p8l1D6snXEvhc1aR",
          "name": "Prod_MsGraph_OAuth2_Hidar"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "\nfunction safeText(value) { return String(value === undefined || value === null ? '' : value).trim(); }\nfunction num(value, fallback) { const n = Number(value); return Number.isFinite(n) ? n : fallback; }\nfunction berlinNow() {\n  const f = new Intl.DateTimeFormat('en-CA', { timeZone: 'Europe/Berlin', year:'numeric', month:'2-digit', day:'2-digit', hour:'2-digit', minute:'2-digit', second:'2-digit', hour12:false });\n  const p = {}; for (const part of f.formatToParts(new Date())) { if (part.type !== 'literal') p[part.type] = part.value; }\n  return `${p.year}-${p.month}-${p.day}T${p.hour}:${p.minute}:${p.second}`;\n}\nfunction httpError(item) {\n  const j = item && item.json ? item.json : {};\n  if (j.error) {\n    const e = j.error.error || j.error;\n    return (e && (e.message || e.code)) ? (e.message || e.code) : 'Microsoft Graph request failed';\n  }\n  return '';\n}\n\nconst err = httpError(items[0]);\nif (err) { return [{ json: { __empty:true, response:'Tool failed: Yes\\nTool used: WF_SAVE_TODAY_UNREAD_ATTACHMENTS_TO_ONEDRIVE_ROOT\\nError message: ' + err + '\\nAction taken: None\\nVerification status: Failed', verificationStatus:'Failed' } }]; }\nconst data = items[0].json || {};\nconst msgs = Array.isArray(data.value) ? data.value : [];\nconst out = [];\nfor (const m of msgs) {\n  const atts = Array.isArray(m.attachments) ? m.attachments : [];\n  for (const a of atts) {\n    if (a.isInline || !a.contentBytes) continue;\n    const safeName = String(a.name || 'attachment').replace(/[\\\\/:*?\"<>|]/g,'_');\n    const uploadUrl = 'https://graph.microsoft.com/v1.0/me/drive/root:/' + encodeURIComponent(safeName) + ':/content?@microsoft.graph.conflictBehavior=fail';\n    out.push({ json: { upload_url: uploadUrl, name: safeName, size: a.size || 0, contentType: a.contentType || 'application/octet-stream' }, binary: { data: { data: a.contentBytes, fileName: safeName, mimeType: a.contentType || 'application/octet-stream' } } });\n  }\n}\nif (out.length === 0) { return [{ json: { __empty:true, response:'No unread attachments from today were found in the Inbox.\\nVerification status: Passed', verificationStatus:'Passed' } }]; }\nreturn out;\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        660,
        0
      ],
      "id": "aa65070a-b3a1-46c7-9875-6c2b20b96518",
      "name": "02 Build Upload Items"
    },
    {
      "parameters": {
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "options": {},
        "method": "PUT",
        "url": "={{ $json.upload_url }}",
        "sendBody": true,
        "contentType": "binaryData",
        "inputDataFieldName": "data",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "={{ $json.contentType }}"
            }
          ]
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        880,
        0
      ],
      "id": "f470750e-fd2a-4358-b7b7-f3899900bd17",
      "name": "03 Upload To OneDrive Root",
      "credentials": {
        "oAuth2Api": {
          "id": "p8l1D6snXEvhc1aR",
          "name": "Prod_MsGraph_OAuth2_Hidar"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "\nfunction safeText(value) { return String(value === undefined || value === null ? '' : value).trim(); }\nfunction num(value, fallback) { const n = Number(value); return Number.isFinite(n) ? n : fallback; }\nfunction berlinNow() {\n  const f = new Intl.DateTimeFormat('en-CA', { timeZone: 'Europe/Berlin', year:'numeric', month:'2-digit', day:'2-digit', hour:'2-digit', minute:'2-digit', second:'2-digit', hour12:false });\n  const p = {}; for (const part of f.formatToParts(new Date())) { if (part.type !== 'literal') p[part.type] = part.value; }\n  return `${p.year}-${p.month}-${p.day}T${p.hour}:${p.minute}:${p.second}`;\n}\nfunction httpError(item) {\n  const j = item && item.json ? item.json : {};\n  if (j.error) {\n    const e = j.error.error || j.error;\n    return (e && (e.message || e.code)) ? (e.message || e.code) : 'Microsoft Graph request failed';\n  }\n  return '';\n}\n\nconst first = items[0] ? items[0].json : {};\nif (first && first.__empty) { return [{ json: { status: first.verificationStatus==='Passed'?'ok':'failed', response: first.response, verificationStatus: first.verificationStatus } }]; }\nlet saved=0, skipped=0; const savedNames=[]; const skippedNames=[];\nfor (const it of items) { const j=it.json||{}; if (j.error) { skipped++; skippedNames.push(j.name||'attachment'); } else { saved++; savedNames.push(j.name||'attachment'); } }\nreturn [{ json: { status:'ok', saved, skipped, response:`Saved today's unread attachments to OneDrive root.\\nSaved: ${saved} ${savedNames.length?('('+savedNames.join(', ')+')'):''}\\nSkipped duplicates/errors: ${skipped} ${skippedNames.length?('('+skippedNames.join(', ')+')'):''}\\nExisting files were not overwritten.\\nVerification status: ${saved>0?'Passed':'Failed'}`, verificationStatus: saved>0?'Passed':'Failed' } }];\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1100,
        0
      ],
      "id": "22aa2040-9e9a-4367-86a9-049d051eb810",
      "name": "04 Format Save Result"
    }
  ],
  "pinData": {},
  "connections": {
    "When Executed by Another Workflow": {
      "main": [
        [
          {
            "node": "00 Prepare Today Range",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "00 Prepare Today Range": {
      "main": [
        [
          {
            "node": "01 Get Today Unread With Attachments",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "01 Get Today Unread With Attachments": {
      "main": [
        [
          {
            "node": "02 Build Upload Items",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "02 Build Upload Items": {
      "main": [
        [
          {
            "node": "03 Upload To OneDrive Root",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "03 Upload To OneDrive Root": {
      "main": [
        [
          {
            "node": "04 Format Save Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate",
    "availableInMCP": false
  },
  "versionId": "47d4b71a-5e74-4997-9373-d440889973ce",
  "meta": {
    "instanceId": "030officeworkflowaiassistantcleanbuild0000000000000000000000"
  },
  "nodeGroups": [],
  "id": "54358e59d2ef4c",
  "tags": []
}