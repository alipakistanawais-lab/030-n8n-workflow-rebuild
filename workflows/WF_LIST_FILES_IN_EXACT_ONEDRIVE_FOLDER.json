{
  "name": "WF_LIST_FILES_IN_EXACT_ONEDRIVE_FOLDER",
  "nodes": [
    {
      "parameters": {
        "workflowInputs": {
          "values": [
            {
              "name": "folderPath"
            },
            {
              "name": "limit"
            }
          ]
        }
      },
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        0
      ],
      "id": "162a0653-fca3-4122-843d-0e2ca4ade72f",
      "name": "When Executed by Another Workflow"
    },
    {
      "parameters": {
        "jsCode": "\nfunction safeText(value) { return String(value === undefined || value === null ? '' : value).trim(); }\nfunction num(value, fallback) { const n = Number(value); return Number.isFinite(n) ? n : fallback; }\nfunction berlinNow() {\n  const f = new Intl.DateTimeFormat('en-CA', { timeZone: 'Europe/Berlin', year:'numeric', month:'2-digit', day:'2-digit', hour:'2-digit', minute:'2-digit', second:'2-digit', hour12:false });\n  const p = {}; for (const part of f.formatToParts(new Date())) { if (part.type !== 'literal') p[part.type] = part.value; }\n  return `${p.year}-${p.month}-${p.day}T${p.hour}:${p.minute}:${p.second}`;\n}\nfunction httpError(item) {\n  const j = item && item.json ? item.json : {};\n  const container = j.error || (j.body && j.body.error) || (j.response && j.response.body && j.response.body.error);\n  if (!container) return '';\n  function pick(o) {\n    if (!o) return '';\n    if (typeof o === 'string') return o.trim();\n    if (o.message) return String(o.message).trim();\n    if (o.description) return String(o.description).trim();\n    if (o.error) return pick(o.error);\n    if (o.code) return String(o.code).trim();\n    return '';\n  }\n  const msg = pick(container) || (typeof j.message === 'string' ? j.message.trim() : '');\n  if (msg) return msg;\n  try { const s = JSON.stringify(container); if (s && s !== '{}') return s.slice(0, 300); } catch (e) {}\n  return 'Microsoft Graph request failed';\n}\n\nconst input = items[0].json || {};\nlet folder = safeText(input.folderPath).replace(/\\s+\\/\\s+/g,'/').replace(/\\\\/g,'/').replace(/\\/+/g,'/').replace(/^\\/|\\/$/g,'');\nlet limit = num(input.limit, 25); if (limit<1) limit=25; if (limit>100) limit=100;\nif (!folder) { return [{ json: { __invalid:true, response:'Which exact OneDrive folder should I list?\\nVerification status: Failed', verificationStatus:'Failed' } }]; }\nconst encPath = folder.split('/').map(encodeURIComponent).join('/');\nreturn [{ json: { __invalid:false, folder, url: 'https://graph.microsoft.com/v1.0/me/drive/root:/' + encPath + ':/children?$top=' + limit + '&$select=id,name,size,webUrl,lastModifiedDateTime,file,folder&$orderby=lastModifiedDateTime desc' } }];\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        0
      ],
      "id": "3336f283-eed6-4f3d-8982-6a47fda0b1cf",
      "name": "00 Prepare Input"
    },
    {
      "parameters": {
        "authentication": "genericCredentialType",
        "genericAuthType": "oAuth2Api",
        "options": {},
        "url": "={{ $json.__invalid ? 'https://graph.microsoft.com/v1.0/me' : $json.url }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        440,
        0
      ],
      "id": "b56af5d5-ef43-4860-a8ed-b50605704886",
      "name": "01 Microsoft Graph Request",
      "credentials": {
        "oAuth2Api": {
          "id": "p8l1D6snXEvhc1aR",
          "name": "Prod_MsGraph_OAuth2_Hidar"
        }
      },
      "alwaysOutputData": true,
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "\nfunction safeText(value) { return String(value === undefined || value === null ? '' : value).trim(); }\nfunction num(value, fallback) { const n = Number(value); return Number.isFinite(n) ? n : fallback; }\nfunction berlinNow() {\n  const f = new Intl.DateTimeFormat('en-CA', { timeZone: 'Europe/Berlin', year:'numeric', month:'2-digit', day:'2-digit', hour:'2-digit', minute:'2-digit', second:'2-digit', hour12:false });\n  const p = {}; for (const part of f.formatToParts(new Date())) { if (part.type !== 'literal') p[part.type] = part.value; }\n  return `${p.year}-${p.month}-${p.day}T${p.hour}:${p.minute}:${p.second}`;\n}\nfunction httpError(item) {\n  const j = item && item.json ? item.json : {};\n  const container = j.error || (j.body && j.body.error) || (j.response && j.response.body && j.response.body.error);\n  if (!container) return '';\n  function pick(o) {\n    if (!o) return '';\n    if (typeof o === 'string') return o.trim();\n    if (o.message) return String(o.message).trim();\n    if (o.description) return String(o.description).trim();\n    if (o.error) return pick(o.error);\n    if (o.code) return String(o.code).trim();\n    return '';\n  }\n  const msg = pick(container) || (typeof j.message === 'string' ? j.message.trim() : '');\n  if (msg) return msg;\n  try { const s = JSON.stringify(container); if (s && s !== '{}') return s.slice(0, 300); } catch (e) {}\n  return 'Microsoft Graph request failed';\n}\n\nconst prep = $('00 Prepare Input').item.json;\nif (prep.__invalid) { return [{ json: { status:'failed', response: prep.response, verificationStatus:'Failed' } }]; }\nconst item = items[0]; const err = httpError(item);\nif (err) { return [{ json: { status: 'failed', response: 'Tool failed: Yes\\n' + 'Tool used: WF_LIST_FILES_IN_EXACT_ONEDRIVE_FOLDER\\n' + 'Error message: ' + (err) + '\\n' + 'Action taken: None\\n' + 'Verification status: Failed', verificationStatus: 'Failed' } }]; }\nconst rows = Array.isArray((item.json||{}).value)?item.json.value:[];\nif (rows.length === 0) { return [{ json: { status:'ok', count:0, response:`The folder \"${prep.folder}\" is empty or returned no files.\\nVerification status: Passed`, verificationStatus:'Passed' } }]; }\nconst lines = rows.map((r,i) => `${i+1}. ${r.folder?'[Folder] ':''}${r.name}${r.file?(' ('+(r.size||0)+' bytes)'):''}\\n   Link: ${r.webUrl||''}`);\nreturn [{ json: { status:'ok', count:rows.length, response:`Files in \"${prep.folder}\" (${rows.length}):\\n` + lines.join('\\n') + '\\nVerification status: Passed', verificationStatus:'Passed' } }];\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        660,
        0
      ],
      "id": "dee55c84-e72f-4cf5-af7b-772dbef68f13",
      "name": "02 Format Result"
    }
  ],
  "pinData": {},
  "connections": {
    "When Executed by Another Workflow": {
      "main": [
        [
          {
            "node": "00 Prepare Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "00 Prepare Input": {
      "main": [
        [
          {
            "node": "01 Microsoft Graph Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "01 Microsoft Graph Request": {
      "main": [
        [
          {
            "node": "02 Format Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate",
    "availableInMCP": false
  },
  "versionId": "90820d7d-56c4-4ed1-b612-2dc786062e19",
  "meta": {
    "instanceId": "030officeworkflowaiassistantcleanbuild0000000000000000000000"
  },
  "nodeGroups": [],
  "id": "ed5721f88ae54f",
  "tags": []
}